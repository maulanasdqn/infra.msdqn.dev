# starship

`default.nix` is a NixOS/Darwin wrapper around the reusable `./hm.nix`
home-manager module, so the same prompt config can be imported directly by
single-user home-manager (nix-on-droid / `honor`).

See `../README.md` for the wrapper pattern.
