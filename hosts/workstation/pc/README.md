# pc — desktop workstation

NixOS workstation.

## `hardware.nix` is a placeholder

**TODO:** replace the placeholders with the real output of

```sh
nixos-generate-config --show-hardware-config
```

run on the actual PC — kernel modules, filesystem UUIDs, swap and CPU vendor.
Until that is done this host will not boot correctly on real hardware.
